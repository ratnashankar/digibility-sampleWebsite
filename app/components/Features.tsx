"use client";

import React from "react";

interface FeatureItem {
  title: string;
  desc: string;
}

export default function Features() {
  return <></>; // 🔥 ONE LINE → hides whole UI & prevents Next.js errors
}
